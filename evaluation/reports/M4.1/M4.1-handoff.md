# M4.1 → 后续阶段交接

## 已具备

- 冻结数据、政策、Fixture、manifest 和 reviewer gate。
- Component 与 Integration 两条离线评测视图。
- Golden Oracle 驱动的确定性评分，避免运行结果自带期望值污染。
- Semantic-only Live 路径及阻断/观察案例选择。
- 不可覆盖的 M4-before 基线和四类配置 fingerprint。
- Offline acceptance 与正式 all acceptance 的可重复命令。

## 下一步前置条件

1. 当前实现已提交，source tree 已 clean。
2. 在安全环境使用开发期 launcher 从 `.env` 注入 `DEEPSEEK_API_KEY` 与 `DEEPSEEK_MODEL`，保持 strict 配置：无 retry、无 cache、无 fallback、无 partial success。
3. 可复现执行：

   ```powershell
   venv\Scripts\python -m evaluation.stage_acceptance --stage M4.1 --mode all
   ```

4. 本次已得到 `LIVE_BASELINE_SUCCESS`、20 次语义调用和 0 个 evaluator error，M4.1 正式验收结论已完成。
5. 后续按路线图判断 M4.2；不得用本次 M4.1 结果替代 M4.2 的独立验收。

## 已知差距

- Component 当前明确停在 `SCOPE`，这是 ScopeResolver 尚未实现的基线，不是通过。
- Integration 能真实执行当前 M4 公开链，但对正式商务差旅 Oracle 的业务断言仍全部为 `FAIL`，后续应由 M4.2 路由/语义能力解决。
- 当前全仓 mypy 仍会报历史 `src` 类型错误；本次新增评测核心文件自身已通过定向 mypy，未修改生产业务语义以掩盖该债务。
- `M4-before.json` 为不可变内容；若数据、政策、Fixture 或 runner 变化，应产生新版本或显式重新冻结，不能覆盖旧基线。

## 关键文件

- 评测日志：[M4.1-development-log.md](</F:/Commercial project/skill/.codex/project-log/M4.1-development-log.md>)
- 完成报告：[M4.1-completion.md](</F:/Commercial project/skill/evaluation/reports/M4.1/M4.1-completion.md>)
- 离线基线：[M4-before.json](</F:/Commercial project/skill/evaluation/reports/M4.1/M4-before.json>)

## 2026-08-13 Formal acceptance result

- M4.1 formal acceptance: `PASS` for the evaluation-baseline gates.
- Reproducible development-time command:
  ```powershell
  venv\Scripts\python -m evaluation.run_m4_1_acceptance
  ```
  The launcher reads `.env` and passes a configuration allowlist to the child process. It does not print or persist secret values. Production configuration loading remains explicit-environment-only.
- Recorded result: `LIVE_BASELINE_SUCCESS`, `run_status=COMPLETED`, `semantic_runs=20`, exit code `0`, no evaluator errors, no downstream calls, and no retries.
- `business_assertion=FAIL` in the Live artifact is intentionally retained as the current M4 business-semantic baseline. It shows the unresolved Interpreter/semantic routing gap and is not converted into a false pass. M4.2 must address it.
