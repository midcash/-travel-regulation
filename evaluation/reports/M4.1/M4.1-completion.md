# M4.1 商务差旅评测基线完成报告

## 结论

M4.1 的离线评测基线、错误根因可观测性和正式验收入口已完成。正式 `all` 验收已于 2026-08-13 通过；本报告后文保留初次尝试记录和最终结果。

## 已交付

- 12 条正式 Golden Case、6 条开发 Case、Dataset Card、Reviewer Gate、manifest、Fixture 和 5 份版本化政策。
- 类型化 Case/Oracle/Run/Scorecard 合同与只读 Case Registry。
- 不依赖 LLM 的确定性 Scorer，显式区分 `PASS`、业务 `FAIL` 和 `NOT_SCORABLE`，并记录首失败阶段与失败分类。
- Component Offline Runner、真实公开 M4 组合入口的 Stage Integration Runner，以及受控 Live Semantic Runner。
- 四类 fingerprint、凭证与 URL query 脱敏、不可覆盖 Artifact、baseline/current/diff 报告能力。
- `evaluation.stage_acceptance --stage M4.1 --mode offline|all` 统一验收入口。

## M4-before 离线结果

结果文件：[M4-before.json](</F:/Commercial project/skill/evaluation/reports/M4.1/M4-before.json>)。

- Component：12/12 完成运行；当前 ScopeResolver 未在本阶段实现，因此 12/12 标记 `NOT_IMPLEMENTED_IN_BASELINE`，首失败 `SCOPE`，evaluator error 为 0。
- Stage Integration：12/12 完成当前 M4 组合链；12/12 标记 `IMPLEMENTED`，Golden Oracle 业务断言均为 `FAIL`，首失败分布归于 `INTERPRETER`，evaluator error 为 0。
- 这些业务失败是当前 M4 对商务差旅 Golden Oracle 的真实基线事实，不是评测框架错误，也不被综合分数抵消。

## 验证

- M4.1 专项：`45 passed, 1 deselected`。
- 全仓：`682 passed, 9 deselected`。
- Ruff：评测实现及 M4.1 测试通过。
- 评测核心 mypy：contracts、registry、scoring、fingerprints、artifacts、reporting、policies 通过；全 `mypy evaluation` 未报告 evaluation 文件错误，但会加载仓库既有 `src` 类型债务。
- Offline acceptance：dataset contract、evaluator correctness、determinism、component/integration baseline、M4-before、self-diff 全部 `PASS`。
- Live slow 测试：未提供真实凭证时按设计 `1 skipped`。

## 初次正式门尝试（历史记录）

初次直接运行 `--mode all` 返回 `NON_ACCEPTANCE_RUN`，原因是工作树 dirty，因此按验收规则没有启动 Live，也没有复用旧 Live 成功结果。随后通过开发期 `.env` launcher 在 clean source tree 中完成正式运行，最终结果见下方正式验收更新。

## 当前实现限制

- M4.1 只建立评测基线；ScopeResolver、商务差旅行程语义修复、真实供应商接入和政策推理不在本次实现范围内。
- Integration 使用显式离线 Fake provider/Interpreter/Composer 验证当前 M4 组合链，不代表真实外部供应商或真实 LLM 质量。
- M4.2 仍须等待其自身规格、实现和验收，不因本次 M4.1 通过而提前进入阶段验收。
## 2026-08-13 Formal acceptance update

This section supersedes the preliminary Live status above.

- Development-time Live launcher: `venv\Scripts\python -m evaluation.run_m4_1_acceptance`.
- The launcher reads the repository `.env` file, injects only the acceptance configuration into the child process, never prints secret values, and leaves production `load_settings()` explicit-environment-only.
- Formal command result: Dataset Contract `PASS`; Evaluator Correctness `PASS`; Offline Determinism `PASS`; Component Baseline `PASS`; Stage Integration Baseline `PASS`; M4-before `PASS`; Self Diff `PASS`; Live `LIVE_BASELINE_SUCCESS`.
- Live artifact result: `run_status=COMPLETED`, `semantic_runs=20`, 20 case results, `retry_count=0`, `downstream_calls=0`, and no evaluator error. The process returned exit code `0`.
- The 20 Golden Oracle business assertions remain `FAIL` because the current M4 semantic chain still has the documented travel-intent gaps (for example traveler extraction and business-travel capability routing). These are the M4.2 semantic baseline, not evaluator failures, and were not relabeled as acceptance success.
- No retry, fallback, cache, synthetic success, partial success, or external write action was introduced.

Therefore M4.1 formal acceptance is complete for its stated evaluation-baseline and observability gates. M4.2 remains required for the business-semantic Oracle failures.
