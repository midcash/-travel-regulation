{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Evaluate Plan Request",
  "description": "Orchestrator → Evaluation Agent: 方案质量评估请求的 payload schema (Mode B/C)",
  "type": "object",
  "required": ["draft", "mode"],
  "properties": {
    "draft": {
      "type": "object",
      "description": "待评估的行程草稿"
    },
    "mode": {
      "type": "string",
      "enum": ["B", "C"],
      "description": "评估模式: B=业务产出评估, C=贡献度评估(消融实验)"
    },
    "previous_report": {
      "type": "object",
      "description": "前一轮评估报告（修订闭环中使用）"
    },
    "dimensions": {
      "type": "object",
      "description": "评估维度权重配置"
    }
  }
}
