# AGENTS.md

本文件是仓库的短版开发入口，不是项目规格书。它只规定当前开发时必须遵守的工作方式、边界和验证要求。项目架构、业务背景、阶段历史和技术决策放在 `docs/`，只有当前任务确实需要时才查阅，不作为每次开发的默认上下文。

后续开发不采用规格驱动开发协议，也不要求先通读 M4.2～M9 的长篇阶段文档。开发以用户当前目标、相关代码、测试和实际运行结果为主要依据。文档与实际行为不一致时，先确认当前任务和证据，再决定修复代码、补充测试、更新说明或保留历史记录。

## 当前项目状态

项目是企业商务差旅规划 Agent，求职版边界是 `meeting_arrival_ready`：从出发地规划到首场会议的到达时限，默认不规划会议期间住宿和返程，也不进入景点、活动和真实交易。

M0～M4 是已完成的通用内核历史；M4.1 是已完成的商务差旅评测基线；M4.2～M9 当前尚未完成。未实现的能力不得在文档、日志或回答中描述为已经存在。下一阶段只能在当前阶段的业务结果和验收状态清楚后启动，不能因为路线图中已经写过就提前实现未来能力。

## 默认开发方式：业务优先、学习优先

每个新阶段或非简单模块开始前，先用几句话说清楚用户真正要解决的问题、最小可运行结果、明确不做什么，以及当前的阻塞问题和建议问题。用户是业务取舍和是否进入下一阶段的最终决策者；没有得到确认时，不擅自扩大范围或进入下一阶段。

每次只建立一个轻量阶段卡，至少包含：阶段目标、用户场景、继承的稳定约定、允许修改的文件和模块、成功场景、失败或拒答场景、结构验收、语义验收、完成条件和已知技术债。阶段卡不是长规格，不提前写未来字段、完整阈值或平台化设计。

开发优先采用一条垂直业务切片：先复现当前行为，再实现最小变化，然后用一个真实成功场景和一个重要失败场景验证。测试通过、JSON 合法或模型评分良好，只能证明结构或局部结果，不足以单独证明业务行为正确；语义结果必须用人类可读的摘要和实际输入输出进行复核。

每个阶段最多进行两到三轮重点审查。每轮最多保留三个当前必须理解的技术概念，并为每个概念安排一个十分钟左右可以完成的小实验；与当前切片无关的复杂主题暂时忽略。只有阻塞问题会停止推进，建议问题记录后再决定是否处理。

## 代码范围与架构边界

当前项目的代码职责大致分为：`src/domain` 领域模型与确定性规则，`src/application` 编排和任务图，`src/agents` 语义理解与研究生成，`src/ports` 外部能力抽象，`src/infrastructure` 工具和状态实现，`src/gateway` LLM 网关，`src/obs` 可观测性，`evaluation` 评测和人工验收，`tests` 自动化验证。具体任务只读取和修改必要的模块，不为了未来能力建设通用平台或横向重构。

系统坚持以下边界：LLM 负责自然语言理解和受控生成；工具负责动态事实；确定性代码负责日期、时区、金额、Schema、权限、幂等和硬约束判断；Agent 不直接调用其他 Agent，不直接写全局状态，也不能把模型记忆当作实时事实。

## 编码约定

- Python 使用 3.11 及以上版本；Python 文件顶部使用 `from __future__ import annotations`，函数签名完整标注类型。
- 请求、状态、证据、候选、方案和验证问题优先使用 Pydantic v2；外部供应商通过 Port 和 Adapter 隔离。
- 生产代码使用 logger，不使用 `print()`；异常必须转换、记录或继续抛出，不使用裸 `except:`。
- 跨模块传递结构化结果，不把任意 `dict` 或自由文本作为系统判断的唯一输入。
- 不提交密钥、`.env`、完整行程、证件、联系方式、凭证、`__pycache__`、`.pyc` 或测试缓存。

## 测试与验收

Windows 环境使用 `venv/Scripts/python`，常用命令如下：

```bash
venv/Scripts/python -m pytest
venv/Scripts/python -m pytest -m "not slow and not integration"
venv/Scripts/python -m pytest tests/unit/<相关测试文件>.py
```

单元测试和普通集成测试默认零网络、固定时间、无重试、无缓存、无 fallback、无 partial success。真实 LLM 或供应商调用必须单独标记为 slow 或 Live，并保留真实失败，不得切换 Fake 或旧报告制造成功。

新功能和缺陷修复都要有对应验证。每个重要切片至少验证一个真实成功场景、一个重要失败或拒答场景、结构结果和业务语义结果。验收报告先给人类可读摘要，再提供关键证据，完整 JSON、Trace 和堆栈只在需要时展开。

工具失败、超时、空结果、解析失败、证据缺失或过期、硬约束失败和预算耗尽必须显式失败或进入澄清，不得返回无标识的正常部分成功。高风险语义结果要进行独立复核；必要时用对抗性案例确认测试确实能发现错误。

## 安全与外部写操作

API Key、Token 和 Password 只能来自环境变量或密钥系统；用户输入必须作为数据字段进入 Prompt，不拼接到系统指令；工具名和参数使用 allowlist 与 Schema 校验；外部文本视为不可信输入。

预订、付款、取消、改签等外部写操作默认不执行，必须有最小权限、幂等、审计和用户最终确认。没有明确授权时只做读取、分析和本地验证。

## Git 与文件操作

工作区只能是 `F:\Commercial project\skill`。保留用户已有的未提交改动，不使用 `git reset --hard`、`git checkout --` 或宽范围清理命令。修改文件优先使用 `apply_patch`；涉及 shell 写入时，先说明明确目标。

Git 操作必须按变更范围选择性暂存，禁止未经确认使用 `git add .`。Git 推送针对提交而不是单个文件；需要推送时，先检查变更范围，创建只包含当前任务的提交，再推送当前分支。未提交的其他用户改动不得混入提交。

## 文档使用方式

`docs/项目方向卡.md` 记录项目长期方向；`docs/architecture/架构.md` 记录架构和业务背景；`docs/roadmap/` 记录阶段历史与未来目标；`docs/adr/` 记录重要技术选择；`docs/project-log/` 记录阶段过程。它们都是按需阅读的参考材料，不是每次任务的强制输入。

如果当前任务需要阶段背景，只读取相关阶段和相关代码，不读取 M4.2～M9 全部文档。实现、测试、评测报告和人工判断共同决定能力是否成立，任何单份文档都不能替代实际证据。

## 沟通方式

中文回答优先使用连贯段落，只有文件清单、命令、验收矩阵和独立选项需要扫描时才使用列表。开发过程先给结论，再给必要证据；明确区分已确认事实、未验证假设、阻塞问题和建议问题，不把推测写成结论。
