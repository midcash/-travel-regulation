[pytest]
markers =
    unit: 不跨进程或外部边界的快速单元测试
    contract: Fake 与真实实现共享的行为契约测试
    integration: 多个本地组件组合的零网络集成测试
    e2e: 从入口到结果的零网络端到端测试
    live_llm: M0 requires a real DeepSeek API smoke test
    slow: 真实 API 或明确耗时的测试，默认不运行
addopts = -m "not slow"
testpaths = tests
